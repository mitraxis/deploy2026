import React from 'react'
import ReactDOM from 'react-dom/client'
import SoloProtocol from './solo-deployment-protocol.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SoloProtocol />
  </React.StrictMode>,
)